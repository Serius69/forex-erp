import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const PredictionsChart: React.FC = () => (
  <Card>
    <CardContent>
      <Typography variant="h6">Predicciones de Tasas</Typography>
      <Typography color="text.secondary">Gráfico en construcción</Typography>
    </CardContent>
  </Card>
);

export default PredictionsChart;