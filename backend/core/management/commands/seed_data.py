# This file is intentionally empty.
# The active seed_data command lives in:
#   backend/users/management/commands/seed_data.py
# (users app is in INSTALLED_APPS; core app is not, so Django ignores this file)
